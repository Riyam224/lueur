/// Values of [MoodEntryEntity.entryType] — plain strings (JSON contract
/// with the backend), centralized here to avoid duplicated literals.
class MoodEntryType {
  static const String moodChat = 'mood_chat';
  static const String breathing = 'breathing';
  static const String sudoku = 'sudoku';
  static const String drawing = 'drawing';
}
